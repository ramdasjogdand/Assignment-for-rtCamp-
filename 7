

delete_site() {
    local site_name=$1
    
    docker-compose -f ${site_name}/docker-compose.yml down
    
    rm -rf ${site_name}

    echo "Site ${site_name} deleted."
}

if [ "$1" == "delete" ]; then
    delete_site "$2"
else
    echo "Usage: delete_site.sh delete [site_name]"
fi
